# =============================================
# DoctorConnect - Application Configuration
# =============================================

# Server
server.port=8080

# ---- PostgreSQL DataSource ----
spring.datasource.url=jdbc:postgresql://localhost:5433/quickdoctor
spring.datasource.username=emsuser
spring.datasource.password=emsuser
spring.datasource.driver-class-name=org.postgresql.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ---- JWT Configuration ----
app.jwt.secret=DoctorConnectSecretKeyForJWTTokenGeneration2024MustBeAtLeast256BitsLong!!
app.jwt.expiration-ms=86400000
app.jwt.remember-me-expiration-ms=604800000

# ---- OTP Configuration ----
app.otp.expiration-minutes=10

# ---- Gmail SMTP ----
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=kbcwork11@gmail.com
spring.mail.password=qwsg tmww mbbx sheo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ---- File Upload ----
app.file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ---- Swagger / OpenAPI ----
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# ---- Logging ----
logging.level.com.doctorconnect=DEBUG
logging.level.org.springframework.security=DEBUG
